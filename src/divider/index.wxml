<view class="wux-class wux-divider {{ dashed ? 'wux-divider--dashed' : '' }} {{ showText ? 'wux-divider--text' : '' }} {{ showText && position ? 'wux-divider--text-' + position : '' }}">
	<view class="wux-divider__text" wx:if="{{ showText }}">
		{{ text }}
		<slot></slot>
	</view>
</view>
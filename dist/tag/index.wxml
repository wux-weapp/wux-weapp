<view class="wux-class wux-tag {{ className }}" hover-class="wux-tag--hover" style="{{ tagStyle }}" bindtap="onClick" wx:if="{{ tagVisible }}">
    <slot></slot>
    <view class="wux-tag__icon" catchtap="onClose" wx:if="{{ closable }}"></view>
</view>
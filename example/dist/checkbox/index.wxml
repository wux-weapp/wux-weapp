<wux-cell title="{{ title }}" label="{{ label }}" extra="{{ extra }}">
    <wux-checkbox-item slot="header" wux-class="wux-checkbox--cell" value="{{ value }}" checked="{{ inputChecked }}" color="{{ color }}" disabled="{{ disabled }}" controlled bind:change="checkboxChange" />
</wux-cell>